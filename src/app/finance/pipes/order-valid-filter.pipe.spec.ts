import { OrderValidFilterPipe } from './order-valid-filter.pipe';

describe('OrderValidFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderValidFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
