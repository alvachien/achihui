import { UIAccountCtgyFilterExPipe } from './uiaccount-ctgy-filterex.pipe';

describe('UIAccountCtgyFilterExPipe', () => {
  it('create an instance', () => {
    const pipe: any = new UIAccountCtgyFilterExPipe();
    expect(pipe).toBeTruthy();
  });
});
