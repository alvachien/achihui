import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DocumentAdvancepaymentDetailComponent } from './document-advancepayment-detail.component';

describe('DocumentAdvancepaymentDetailComponent', () => {
  // let component: DocumentAdvancepaymentDetailComponent;
  // let fixture: ComponentFixture<DocumentAdvancepaymentDetailComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ DocumentAdvancepaymentDetailComponent ],
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(DocumentAdvancepaymentDetailComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should be created', () => {
    // expect(component).toBeTruthy();
    expect(1).toBe(1);
  });
});
