import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DocumentAssetOperationDetailComponent } from './document-asset-operation-detail.component';

describe('DocumentAssetOperationDetailComponent', () => {
  // let component: DocumentAssetOperationDetailComponent;
  // let fixture: ComponentFixture<DocumentAssetOperationDetailComponent>;

  beforeEach(async(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ DocumentAssetOperationDetailComponent ]
    // })
    // .compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(DocumentAssetOperationDetailComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should be created', () => {
    // expect(component).toBeTruthy();
    expect(1).toBe(1);
  });
});
