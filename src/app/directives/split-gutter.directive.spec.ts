import { SplitGutterDirective } from './split-gutter.directive';

describe('SplitGutterDirective', () => {
  it('should create an instance', () => {
    const directive = new SplitGutterDirective();
    expect(directive).toBeTruthy();
  });
});
